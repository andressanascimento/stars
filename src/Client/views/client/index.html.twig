{% extends 'layout.html.twig' %}
{% block content %}
    <div class="col-md-12">
        <ol class="breadcrumb">
            <li><a href="#">Home</a></li>
            <li class="active">Clientes</li>
        </ol>
        <button class="btn btn-danger pull-right" style="margin-left: 10px;">Deletar</button>
        <button class="btn btn-primary pull-right">Novo</button>
    </div>
    <div class="col-md-3">
        <div class="panel panel-primary"> 
            <div class="panel-heading"> 
                <h3 class="panel-title">Pesquisar</h3> 
            </div> 
            <div class="panel-body">
                <form method="post" name="search" action="/client/client/search">
                    <div class="form-group">
                        <input type="text" name="name" class="form-control" id="name" placeholder="Nome">
                    </div>
                    <button type="submit" class="btn btn-primary">Enviar</button>
                </form> 
            </div> 
        </div>


    </div>
    <div class="col-md-9" id="grid">
        <table class="table table-hover table-condensed" style="margin-top:5px">
            <thead>
                <tr>
                    <th style="width: 30px"></th>
                    <th>Nome</th>
                    <th class="col-md-2">Idade</th>
                    <th class="col-md-1">Editar</th>
                </tr>
            </thead>
            <tbody>
            {% for client in clients %}
                <tr>
                    <td><input type="checkbox" value={{client.id}} class="checkbox-delete"></td>
                    <td>{{client.name}}</td>
                    <td>{{client.age}}</td>
                    <td><button class="btn btn-primary edit" model-id={{client.id}}>Editar</button></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock content %}
