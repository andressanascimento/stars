{% if message is not null %}
    <div class="alert {{message['status']}}" role="alert">{{message['message']}}</div>
{% endif %}
<form method="post" name="search" action="/client/client/form">
    <div class="form-group {% if input_errors['name'] is not null %} has-error {% endif %}">
        <input type="text" name="name" class="form-control" placeholder="Nome" value="{% if model is not null %} {{model[0].name}} {% endif %}">
        {% if input_errors['name'] is not null %}
            {% for error in input_errors['name'] %}
                <span class="help-block">{{error}}</span>
            {% endfor %}
        {% endif %}
    </div>
    <div class="form-group {% if input_errors['age'] is not null %} has-error {% endif %}">
        <input type="text" name="age" class="form-control" placeholder="Idade" value="{% if model is not null %} {{model[0].age}} {% endif %}">
        {% if input_errors['age'] is not null %}
            {% for error in input_errors['age'] %}
                <span class="help-block">{{error}}</span>
            {% endfor %}
        {% endif %}
    </div>
    {% if model is not null %}
        <input type="hidden" name="id" value={{model[0].id}} />
    {% endif %}
    <button type="submit" class="btn btn-primary">Enviar</button>
</form>