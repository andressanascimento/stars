{% if message is not null %}
    <br/>
    <div class="alert {{message['status']}}" role="alert" id="alert-message">
        <button type="button" class="close" data-dismiss="alert">x</button>
        {{message['message']}}</div>
{% endif %}
<table class="table table-hover table-condensed" style="margin-top:5px">
    <thead>
        <tr>
            <th style="width: 30px"></th>
            <th>Nome</th>
            <th class="col-md-2">Loja</th>
            <th class="col-md-1">Editar</th>
        </tr>
    </thead>
    <tbody>
    {% for employee in employees %}
        <tr>
            <td><input type="checkbox" value={{employee.id}} class="checkbox-delete"></td>
            <td>{{employee.name}}</td>
            <td>{{employee.store}}</td>
            <td><button class="btn btn-primary btn-modal" modal-title="Atualizar Funcionário"
                        modal-url="/store/employee/update/{{employee.id}}">Editar</button></td>
        </tr>
    {% endfor %}
    </tbody>
</table>